[Unit]
Description="CDS API"

[Service]
User={{ run_user }}
WorkingDirectory=/home/{{ run_user }}/cds/
ExecStart=/home/{{ run_user }}/cds/cds-engine start api --config /home/{{ run_user }}/cds/conf.toml
Restart=always

[Install]
WantedBy=multi-user.target